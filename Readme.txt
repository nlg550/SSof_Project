The project was developed in C++ 14, using Eclipse Oxygen. Also, there is a makefile in the directory so 
it possible to compile using other methods.
To execute the program, use ./SSof_Project <name of the JSON (with the .json)>. 
The output JSON will be created in the same folder and will have .output in the name.